Create an HTML and CSS files that will display a minesweeper game, which we will develop later.

The HTML file should have:

- Title: Minesweeper
- Include a h1 tag with the "Minesweeper" title
- Create the HTML tags for the game, remaining mine count, timer, game status indicator, and placeholder for the game board itself - but do not create the individual game board tiles.

The CSS should be stored in a separate file

